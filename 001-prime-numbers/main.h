#ifndef THRESHOLD_EXERCISES_MAIN_H
#define THRESHOLD_EXERCISES_MAIN_H

#include <stdbool.h>
#include <stdint.h>

int main(void);

uint32_t get_valid_prime_ceiling(void);

bool is_valid_prime_ceiling(uint32_t value);

#endif //THRESHOLD_EXERCISES_MAIN_H
